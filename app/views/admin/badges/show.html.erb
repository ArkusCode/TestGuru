<tr>
  <td class="align-middle"><img src=<%="#{@badge.cover}"%>></td>
  <td class="align-middle"><%= Badge::BADGES_NAMES.key(@badge.title.to_sym) %></td>
  <td><%= @badge.rule %>
</tr>
<p class="mt-3">
  <%= link_to t('.back'), :back, class: 'badge badge-primary' %>
</p>
