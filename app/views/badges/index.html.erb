<div class="container">
  <div class="mt-3">
    <% if @badges.blank? %>
      <h3><%= t('.no_badges_yet') %></h3>
    <% else %>
      <%= render @badges %>
    <% end %>
  </div>
  <div class="mt-3">
    <%= link_to t('.availiable_badges'), show_all_path %>
  </div>
</div>
