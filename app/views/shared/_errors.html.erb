<% if resourse.errors.present? %>
  <div>
    <h2><%= pluralize(resourse.errors.count, 'error') %> detected: </h2>

    <ul>
      <% resourse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
